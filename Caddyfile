idx.loyalme.eu.org {
    encode gzip

    # 静态页面
    root * /admin/public
    file_server

    # Node API 反代
    handle_path /admin/api/* {
        reverse_proxy 127.0.0.1:4000
    }

    handle_errors {
        respond "Not Found" 404
    }
}
